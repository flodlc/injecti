{
  "name": "@flodlc/injekt",
  "author": "flodlc",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "packageManager": "yarn@3.2.1",
  "version": "1.0.0",
  "scripts": {
    "dev": "yarn watch:source && yarn watch:types",
    "watch:types": "npx tsc -w",
    "watch:source": "tsup src/index.ts --watch",
    "build": "yarn run build:types && yarn run build:source",
    "build:types": "npx tsc",
    "build:source": "tsup src/index.ts",
    "test": "jest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^18.7.16",
    "@types/react": "^18.0.19",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.0.2",
    "tsup": "^6.2.3"
  },
  "files": [
    "dist/*"
  ]
}
